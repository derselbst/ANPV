# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

resources:         
  containers:
  - container: qt6Container
    image: mdevriesdekimo/qt6:ubuntu2104
    options: '-v /usr/bin/sudo:/usr/bin/sudo -v /usr/lib/sudo/libsudo_util.so.0:/usr/lib/sudo/libsudo_util.so.0 -v /usr/lib/sudo/sudoers.so:/usr/lib/sudo/sudoers.so -v /etc/sudoers:/etc/sudoers'

pool:
  vmImage: 'ubuntu-20.04'

container: qt6Container

steps:
- checkout: self
  submodules: true
- task: Bash@3
  displayName: 'apt-get update'
  inputs:
    targetType: 'inline'
    script: 'sudo DEBIAN_FRONTEND=noninteractive apt-get update'
- task: Bash@3
  displayName: 'apt-get install'
  inputs:
    targetType: 'inline'
    script: 'sudo DEBIAN_FRONTEND=noninteractive apt-get install -y cmake libtiff-dev libjpeg-dev extra-cmake-modules libkf5kdcraw-dev libexiv2-dev'
- task: CMake@1
  displayName: 'CMake configure'
  inputs:
    cmakeArgs: -DCMAKE_PREFIX_PATH=/opt/Qt ..
    workingDirectory: 'build'

- task: CMake@1
  displayName: 'CMake Build'
  inputs:
    cmakeArgs: --build . --parallel
    workingDirectory: 'build'



